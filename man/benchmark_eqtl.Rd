% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnaseq_api.R
\name{benchmark_eqtl}
\alias{benchmark_eqtl}
\title{Benchmark eQTL recovery}
\usage{
benchmark_eqtl(
  genotype_file,
  expression_file,
  out_prefix,
  sample_metadata = NULL,
  truth_file = NULL,
  gene_metadata = NULL,
  pair_mode = c("truth_neighborhood", "cis", "all"),
  cis_window_bp = 1e+06,
  covariates = c("condition", "batch"),
  test_gxe = TRUE,
  expression_scale = c("auto", "log2", "counts"),
  fdr_threshold = 0.05,
  max_tests = 1e+06
)
}
\arguments{
\item{genotype_file}{Genotype TSV or VCF/VCF.GZ.}

\item{expression_file}{Expression TSV or matrix with genes in rows and
samples in columns. The \code{.expression.tsv} output from
\code{\link[=simulate_rnaseq_from_gwas]{simulate_rnaseq_from_gwas()}} is ready to use.}

\item{out_prefix}{Prefix for association results and benchmark metrics.}

\item{sample_metadata}{Optional data frame or TSV with sample covariates.}

\item{truth_file}{Optional eQTL truth TSV written by
\code{\link[=simulate_rnaseq_from_gwas]{simulate_rnaseq_from_gwas()}}.}

\item{gene_metadata}{Optional data frame or TSV with \code{gene_id}, \code{seqname},
and \code{tss}, required for \code{pair_mode = "cis"}.}

\item{pair_mode}{Candidate-pair strategy: \code{"truth_neighborhood"} tests cis
neighborhoods for truth genes plus causal trans pairs, \code{"cis"} tests all
local pairs, and \code{"all"} tests every pair.}

\item{cis_window_bp}{Cis testing window around each TSS.}

\item{covariates}{Metadata columns to include in each regression.}

\item{test_gxe}{Test genotype-by-condition interaction when exactly two
conditions are present.}

\item{expression_scale}{\code{"log2"}, \code{"counts"}, or \code{"auto"}. Count data are
converted to log2 counts per million.}

\item{fdr_threshold}{Adjusted-p-value threshold used for benchmark metrics.}

\item{max_tests}{Safety limit on the number of variant-gene tests.}
}
\value{
Invisibly returns a list with result and metric paths and tables.
}
\description{
Test genotype-expression associations and compare discoveries with a known
eQTL truth set. Candidate pairs can be restricted to neighborhoods around
simulated causal genes, all cis pairs, or all variant-gene pairs. Batch and
condition covariates are included when available, and a two-condition model
can also test genotype-by-condition interactions.
}
\examples{
\dontrun{
benchmark_eqtl(
  genotype_file = "results/gwas/demo.geno.tsv",
  expression_file = "results/rnaseq/demo.expression.tsv",
  sample_metadata = "results/rnaseq/demo.sample_metadata.tsv",
  truth_file = "results/rnaseq/demo.eqtl_truth.tsv",
  out_prefix = "results/eqtl/demo"
)
}
}
