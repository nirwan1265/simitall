% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrnaseq_api.R
\name{simulate_scrnaseq_from_gwas}
\alias{simulate_scrnaseq_from_gwas}
\title{Simulate donor-aware single-cell RNA-seq from GWAS genotypes}
\usage{
simulate_scrnaseq_from_gwas(
  genotype_file,
  out_prefix,
  annotation_gff3 = NULL,
  sample_metadata = NULL,
  n_genes = NULL,
  cells_per_donor = 100L,
  cell_type_proportions = c(T_cell = 0.35, B_cell = 0.25, Monocyte = 0.25, Stromal =
    0.15),
  trajectory_cell_types = NULL,
  ploidy = 2L,
  n_cis_eqtl = 100L,
  n_trans_eqtl = 25L,
  cis_window_bp = 1e+06,
  cell_type_eqtl_fraction = 0.4,
  condition_levels = c("control", "treatment"),
  batch_levels = c("batch1", "batch2"),
  condition_effect_fraction = 0.1,
  gxe_fraction = 0.2,
  markers_per_cell_type = NULL,
  marker_effect_mean = 1.5,
  marker_effect_sd = 0.3,
  pseudotime_gene_fraction = 0.1,
  pseudotime_effect_sd = 0.5,
  n_latent = 2L,
  baseline_mean_log2 = 5,
  baseline_sd_log2 = 1,
  cis_effect_sd = 0.6,
  trans_effect_sd = 0.35,
  condition_effect_sd = 0.6,
  gxe_effect_sd = 0.4,
  donor_effect_sd = 0.2,
  batch_effect_sd = 0.2,
  latent_effect_sd = 0.25,
  residual_sd = 0.1,
  library_size_mean = 5000,
  library_size_sdlog = 0.35,
  dispersion_mean = 0.1,
  dispersion_shape = 2,
  dropout_rate = 0.05,
  doublet_rate = 0.03,
  ambient_fraction = 0.02,
  backend = c("auto", "splatter", "native"),
  write_dense_tsv = FALSE,
  write_sce = TRUE,
  min_cells_pseudobulk = 5L,
  seed = 1L
)
}
\arguments{
\item{genotype_file}{Genotype TSV or VCF/VCF.GZ. Output from
\code{\link[=simulate_gwas_cohort]{simulate_gwas_cohort()}} can be used directly.}

\item{out_prefix}{Prefix for single-cell outputs.}

\item{annotation_gff3}{Optional gene annotation GFF3. Synthetic genes are
placed along variant coordinates when omitted.}

\item{sample_metadata}{Optional data frame or TSV with a \code{sample} column.
Donor-level population, family, phenotype, condition, and batch columns
are copied into cell metadata.}

\item{n_genes}{Number of genes. With a GFF3, \code{NULL} uses every gene;
otherwise the default is 1000.}

\item{cells_per_donor}{Number of cells per donor, as one value or one value
per donor.}

\item{cell_type_proportions}{Named numeric vector of cell-type proportions.}

\item{trajectory_cell_types}{Cell types assigned continuous pseudotime.
\code{NULL} uses the first cell type; \code{character(0)} disables trajectories.}

\item{ploidy}{Maximum genotype dosage.}

\item{n_cis_eqtl}{Number of causal cis-eQTL genes.}

\item{n_trans_eqtl}{Number of causal trans-eQTL pairs.}

\item{cis_window_bp}{Maximum cis distance from a gene TSS.}

\item{cell_type_eqtl_fraction}{Fraction of eQTLs active in only one cell
type. Remaining eQTLs are shared across cell types.}

\item{condition_levels}{Donor condition labels used when absent from
\code{sample_metadata}.}

\item{batch_levels}{Donor batch labels used when absent from metadata.}

\item{condition_effect_fraction}{Fraction of genes affected by condition.}

\item{gxe_fraction}{Fraction of eQTLs with genotype-by-condition effects.}

\item{markers_per_cell_type}{Number of non-overlapping marker genes assigned
to each cell type.}

\item{marker_effect_mean}{Mean marker effect in log2 units.}

\item{marker_effect_sd}{Marker-effect standard deviation.}

\item{pseudotime_gene_fraction}{Fraction of genes with trajectory effects.}

\item{pseudotime_effect_sd}{Standard deviation of trajectory effects.}

\item{n_latent}{Number of cell-level latent factors.}

\item{baseline_mean_log2}{Mean baseline gene abundance on the log2 scale.}

\item{baseline_sd_log2}{Standard deviation of baseline gene abundance.}

\item{cis_effect_sd}{Standard deviation of cis-eQTL effects.}

\item{trans_effect_sd}{Standard deviation of trans-eQTL effects.}

\item{condition_effect_sd}{Standard deviation of condition effects.}

\item{gxe_effect_sd}{Standard deviation of genotype-by-condition effects.}

\item{donor_effect_sd}{Standard deviation of gene-by-donor effects.}

\item{batch_effect_sd}{Standard deviation of batch effects.}

\item{latent_effect_sd}{Standard deviation of latent-factor loadings.}

\item{residual_sd}{Standard deviation of cell-level log2 residual noise.}

\item{library_size_mean}{Mean UMI library size per cell.}

\item{library_size_sdlog}{Log-scale standard deviation of cell libraries.}

\item{dispersion_mean}{Mean negative-binomial dispersion where
\code{variance = mean + dispersion * mean^2}.}

\item{dispersion_shape}{Gamma shape controlling gene dispersion variation.}

\item{dropout_rate}{Additional expression-dependent dropout probability.}

\item{doublet_rate}{Fraction of cells mixed with a second cell profile.}

\item{ambient_fraction}{Fraction of each expected library drawn from the
global ambient expression pool.}

\item{backend}{Technical scaffold backend: \code{"auto"} uses Splatter when it
is installed and otherwise uses the native gamma/negative-binomial model;
\code{"splatter"} and \code{"native"} force a backend.}

\item{write_dense_tsv}{Also write a dense gene-by-cell count TSV. Sparse
Matrix Market output and an RDS object are always written.}

\item{write_sce}{Write a \code{SingleCellExperiment} RDS when its Bioconductor
package is installed.}

\item{min_cells_pseudobulk}{Minimum cells required for a donor-cell-type
pseudobulk library.}

\item{seed}{Random-number seed.}
}
\value{
Invisibly returns a named list of output paths. The \code{backend}
attribute records the backend actually used.
}
\description{
Generate single-cell UMI counts for the same donors and genotypes used in a
GWAS cohort. The causal model combines cell-type marker programs, shared and
cell-type-specific cis/trans eQTLs, condition effects, genotype-by-condition
interactions, donor effects, trajectories, batches, latent factors, and
technical sampling. The output includes sparse 10x-style files, explicit
truth tables, donor-by-cell-type pseudobulk data, and an optional
\code{SingleCellExperiment} object.
}
\examples{
\dontrun{
sc <- simulate_scrnaseq_from_gwas(
  genotype_file = "results/gwas/demo.geno.tsv",
  out_prefix = "results/scrna/demo",
  sample_metadata = "results/gwas/demo.pheno.tsv",
  n_genes = 500,
  cells_per_donor = 100,
  cell_type_proportions = c(T_cell = 0.35, B_cell = 0.25,
                            Monocyte = 0.25, Stromal = 0.15),
  n_cis_eqtl = 100,
  n_trans_eqtl = 25,
  cell_type_eqtl_fraction = 0.5,
  seed = 1
)
}
}
