% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrnaseq_api.R
\name{simulate_celltype_eqtls}
\alias{simulate_celltype_eqtls}
\title{Assign shared and cell-type-specific activity to causal eQTLs}
\usage{
simulate_celltype_eqtls(
  eqtl_truth,
  cell_types,
  cell_type_specific_fraction = 0.4,
  out_tsv = NULL,
  seed = NULL
)
}
\arguments{
\item{eqtl_truth}{eQTL truth data frame or TSV. It must contain \code{gene_id},
\code{variant_id}, and \code{effect_log2} columns; other columns are preserved.}

\item{cell_types}{Character vector of cell-type labels.}

\item{cell_type_specific_fraction}{Fraction of pairs assigned to one cell
type. Remaining pairs are labelled \code{"shared"} and active in all types.}

\item{out_tsv}{Optional output TSV path.}

\item{seed}{Optional random-number seed. \code{NULL} uses the current RNG stream.}
}
\value{
The augmented eQTL truth data frame, invisibly when written.
}
\description{
Convert a bulk or generic causal eQTL truth table into a single-cell truth
table by marking each pair as shared across all cell types or active in one
selected cell type. This helper is used internally by
\code{\link[=simulate_scrnaseq_from_gwas]{simulate_scrnaseq_from_gwas()}} and can also adapt user-supplied eQTL panels.
}
\examples{
truth <- data.frame(
  gene_id = paste0("gene", 1:4),
  variant_id = paste0("variant", 1:4),
  effect_log2 = c(0.5, -0.4, 0.7, -0.2)
)
cell_truth <- simulate_celltype_eqtls(
  truth,
  cell_types = c("T_cell", "B_cell", "Monocyte"),
  cell_type_specific_fraction = 0.5,
  seed = 1
)
}
