% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnaseq_api.R
\name{simulate_rnaseq_from_gwas}
\alias{simulate_rnaseq_from_gwas}
\title{Simulate RNA-seq from GWAS individuals}
\usage{
simulate_rnaseq_from_gwas(
  genotype_file,
  out_prefix,
  annotation_gff3 = NULL,
  sample_metadata = NULL,
  n_genes = NULL,
  ploidy = 2L,
  n_cis_eqtl = 100L,
  n_trans_eqtl = 25L,
  cis_window_bp = 1e+06,
  condition_levels = c("control", "treatment"),
  batch_levels = c("batch1", "batch2"),
  condition_effect_fraction = 0.1,
  gxe_fraction = 0.2,
  ase_fraction = 0.25,
  n_latent = 3L,
  baseline_mean_log2 = 6,
  baseline_sd_log2 = 1,
  cis_effect_sd = 0.6,
  trans_effect_sd = 0.35,
  condition_effect_sd = 0.6,
  gxe_effect_sd = 0.4,
  batch_effect_sd = 0.2,
  latent_effect_sd = 0.25,
  residual_sd = 0.15,
  library_size_mean = 5e+06,
  library_size_sdlog = 0.25,
  dispersion_mean = 0.1,
  dispersion_shape = 2,
  simulate_reads = FALSE,
  transcript_fasta = NULL,
  transcript_gene_map = NULL,
  read_library_size = 1e+05,
  read_length = 100L,
  paired_end = TRUE,
  seed = 1L
)
}
\arguments{
\item{genotype_file}{Genotype TSV or VCF/VCF.GZ. The TSV written by
\code{\link[=simulate_gwas_cohort]{simulate_gwas_cohort()}} is accepted directly.}

\item{out_prefix}{Prefix for all RNA-seq and truth output files.}

\item{annotation_gff3}{Optional GFF3 containing gene coordinates. When
omitted, synthetic genes are distributed across the variant coordinates.}

\item{sample_metadata}{Optional data frame or TSV with a \code{sample} column.
Existing \code{condition}, \code{batch}, and population columns are preserved.}

\item{n_genes}{Number of genes to simulate. With a GFF3, \code{NULL} uses every
gene; otherwise the default is 1000.}

\item{ploidy}{Number of homologous chromosome copies represented by genotype
dosages. This is used to identify heterozygotes for ASE simulation.}

\item{n_cis_eqtl}{Number of genes assigned one causal cis-eQTL.}

\item{n_trans_eqtl}{Number of causal trans-eQTL pairs.}

\item{cis_window_bp}{Maximum distance in base pairs between a cis variant
and a gene TSS.}

\item{condition_levels}{Character vector of condition labels. Used only when
\code{sample_metadata} does not already contain \code{condition}.}

\item{batch_levels}{Character vector of batch labels. Used only when
\code{sample_metadata} does not already contain \code{batch}.}

\item{condition_effect_fraction}{Fraction of genes with a nonzero condition
effect.}

\item{gxe_fraction}{Fraction of eQTLs with a genotype-by-condition effect.}

\item{ase_fraction}{Fraction of cis-eQTL pairs used for ASE simulation. Set
to zero to disable ASE output.}

\item{n_latent}{Number of unobserved sample-level factors.}

\item{baseline_mean_log2}{Mean baseline log2 abundance.}

\item{baseline_sd_log2}{Standard deviation of baseline log2 abundance.}

\item{cis_effect_sd}{Standard deviation of cis-eQTL effects in log2 units.}

\item{trans_effect_sd}{Standard deviation of trans-eQTL effects in log2
units.}

\item{condition_effect_sd}{Standard deviation of condition effects in log2
units.}

\item{gxe_effect_sd}{Standard deviation of genotype-by-condition effects in
log2 units.}

\item{batch_effect_sd}{Standard deviation of batch effects in log2 units.}

\item{latent_effect_sd}{Standard deviation of latent-factor loadings.}

\item{residual_sd}{Standard deviation of gene-by-sample residual noise on
the log2 abundance scale.}

\item{library_size_mean}{Mean gene-level count library size per sample.}

\item{library_size_sdlog}{Log-scale standard deviation of library sizes.}

\item{dispersion_mean}{Mean negative-binomial dispersion where
\code{variance = mean + dispersion * mean^2}.}

\item{dispersion_shape}{Gamma shape used to vary dispersion among genes.}

\item{simulate_reads}{Also generate FASTQ reads with
\code{\link[=simulate_rnaseq_reads]{simulate_rnaseq_reads()}}.}

\item{transcript_fasta}{Transcript FASTA required when
\code{simulate_reads = TRUE}.}

\item{transcript_gene_map}{Optional transcript-to-gene mapping passed to
\code{\link[=simulate_rnaseq_reads]{simulate_rnaseq_reads()}}.}

\item{read_library_size}{Number of reads or read pairs generated per sample.}

\item{read_length}{Simulated read length.}

\item{paired_end}{Generate paired-end reads.}

\item{seed}{Random-number seed.}
}
\value{
Invisibly returns a named list of output paths.
}
\description{
Generate gene-level RNA-seq counts for the same samples and genotypes used
in a GWAS cohort. Expression is assembled from baseline abundance, cis- and
trans-eQTL effects, condition effects, genotype-by-condition interactions,
batch effects, latent confounders, and residual noise. The function writes
analysis matrices and explicit causal truth sets, including optional
allele-specific expression (ASE) counts.
}
\examples{
\dontrun{
simulate_rnaseq_from_gwas(
  genotype_file = "results/gwas/demo.geno.tsv",
  out_prefix = "results/rnaseq/demo",
  annotation_gff3 = "results/genome.gff3",
  n_cis_eqtl = 200,
  n_trans_eqtl = 50,
  condition_levels = c("control", "drought"),
  batch_levels = c("batch1", "batch2"),
  seed = 1
)
}
}
