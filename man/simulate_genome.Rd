% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/script_wrappers.R
\name{simulate_genome}
\alias{simulate_genome}
\title{Simulate a genome with repeats}
\usage{
simulate_genome(out_fa, in_fa = NULL, random_genome = FALSE, ...)
}
\arguments{
\item{out_fa}{Output FASTA path.}

\item{in_fa}{Optional input FASTA. Omit when \code{random_genome = TRUE}.}

\item{random_genome}{Generate a random genome instead of modifying \code{in_fa}.}

\item{...}{Additional simulator options, such as \code{n_events}, \code{seg_len},
\code{copies}, \code{spacing_distribution}, \code{ploidy}, and \code{snp_rate}.}
}
\value{
Invisibly returns \code{TRUE} after writing the requested files.
}
\description{
Generate a random genome or modify a reference FASTA with tandem repeats,
motif repeats, or both. The simulator can preserve local GC content, control
repeat spacing, generate multiple diverged genome copies, and write
coordinate truth in TSV, BED, GFF3, and JSON formats.
}
\examples{
out <- tempfile(fileext = ".fa")
simulate_genome(
  out_fa = out,
  random_genome = TRUE,
  random_length = 10000,
  n_events = 2,
  seg_len = 100,
  copies = 3,
  seed = 1
)
}
