% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrnaseq_api.R
\name{aggregate_scrnaseq_pseudobulk}
\alias{aggregate_scrnaseq_pseudobulk}
\title{Aggregate single-cell counts into donor-level pseudobulk libraries}
\usage{
aggregate_scrnaseq_pseudobulk(
  counts,
  cell_metadata = NULL,
  out_prefix,
  group_by = c("donor", "cell_type"),
  min_cells = 5L
)
}
\arguments{
\item{counts}{Gene-by-cell matrix, dense count TSV, single-cell result
prefix, or \code{.scrnaseq.rds} path.}

\item{cell_metadata}{Cell metadata data frame or TSV. May be omitted when
\code{counts} identifies a single-cell result object.}

\item{out_prefix}{Output prefix.}

\item{group_by}{Cell metadata columns defining each pseudobulk library.}

\item{min_cells}{Minimum cells required in a retained library.}
}
\value{
Invisibly returns paths to count, expression, and metadata TSVs.
}
\description{
Sum cells within donor, cell type, condition, or other metadata groups and
write raw counts plus log2 counts-per-million expression. Pseudobulk keeps
donors, rather than cells, as the biological replication unit for eQTL and
differential-expression analyses.
}
\examples{
\dontrun{
aggregate_scrnaseq_pseudobulk(
  counts = "results/scrna/demo",
  out_prefix = "results/scrna/demo.pseudobulk",
  group_by = c("donor", "cell_type")
)
}
}
